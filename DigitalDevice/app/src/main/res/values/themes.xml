<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DigitalDevice" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MyCustomTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Đặt gradient làm background của Activity -->
        <item name="android:windowBackground">@drawable/gradient_background</item>
    </style>

    <style name="Theme.DigitalDevice" parent="Base.Theme.DigitalDevice" />
</resources>
